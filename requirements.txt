# cotracker-lips runtime dependencies.
#
# `pip install -e .` reads pyproject.toml and installs everything below
# transitively, so this file is here mainly for users who prefer the legacy
# `pip install -r requirements.txt` workflow.
#
# After installing PyTorch (https://pytorch.org/get-started/locally/) for
# your CUDA version, run:
#     pip install -e .
#     pip install -e cotracker
#     pip install -e SPIGA

customtkinter>=5.2
easygui>=0.98
einops>=0.7
imageio>=2.33
matplotlib>=3.8
moviepy>=1.0
numpy>=1.26,<2.0
opencv-python>=4.9
pandas>=2.2
pillow>=10.3
pyarrow>=15.0
pydub>=0.25
PyYAML>=6.0
retinaface-py>=0.0.2
scipy>=1.15
sort-tracker-py>=1.0.2
spiga>=0.0.6
timm>=0.9
tqdm>=4.67
